{
  "id": "RUSTPY-0013",
  "slug": "lzma-filters-unbounded-collect",
  "title": "RUSTPY-0013 \u2014 lzma filters= eager-collects any iterable (CPython requires a sized sequence) -> unbounded memory / OOM abort",
  "kind": "abort",
  "signatures": [],
  "one_line_repro": "import lzma, itertools; lzma.LZMACompressor(format=lzma.FORMAT_RAW, filters=({'id': lzma.FILTER_LZMA2} for _ in itertools.count()))",
  "repro": "repro.py",
  "vehicles": 1,
  "prior_art": "unreported (eager-collect class; report with RUSTPY-0012)",
  "status": "confirmed",
  "found_in": "fusil-rustpython_07 (eager-collect audit)",
  "confirmed_build": "rustpython 0.5.0 (heads/master:a9c2c529b)",
  "class_note": "notes/unbounded-eager-collect-parity-class.md",
  "notes": "LZMACompressor/LZMADecompressor collect the user's filters= iterable whole (via parse_filter_chain_spec's filter_specs: Vec<PyObjectRef>) before validating; an infinite/huge iterable balloons to OOM abort. CPython calls len() first (requires a sized sequence). Fix = require a list/tuple. Instance of the eager-collect class."
}
